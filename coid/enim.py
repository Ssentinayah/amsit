frames = []  # Initialize an empty array
frame = "some value"  # This could be any value you want to add to the array

frames.push(frame)  # Add the frame to the array

print(frames)  # Print the updated array
